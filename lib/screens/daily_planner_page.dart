import 'package:flutter/material.dart';

class DailyPlannerPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Daily Planner')),
      body: Center(child: Text('Daily Planner Content')),
    );
  }
}
